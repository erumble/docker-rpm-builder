---
- name: Provision nodejs dev box
  hosts: all
  become: yes

  tasks:
    - name: enable epel repo
      yum:
        name: epel-release
        state: installed

    - name: install nodejs and dependencies
      yum:
        name: "{{ item }}"
        state: installed
      with_items:
        - nodejs
        - npm
